# GitHub 账号信息（Nova / 孙子霖）

> 本文件汇总你的 GitHub 关键信息，方便以后随时调用。最后更新：2026-07-23

## 一、账户基本信息
| 项目 | 内容 |
|------|------|
| GitHub 用户名（login） | `NOVASUN168` |
| 个人主页 | https://github.com/NOVASUN168 |
| 账户 ID | `301269417` |
| 注册时间 | 2026-07-08 |
| git 提交用户名 (user.name) | `NOVASUN168` |
| git 提交邮箱 (user.email) | `301269417+NOVASUN168@users.noreply.github.com`（GitHub 隐私邮箱，不暴露真实邮箱） |
| 默认分支 | `main` |

## 二、SSH 密钥（已配置并验证通过）
| 项目 | 内容 |
|------|------|
| 私钥位置 | `C:\Users\zsgre\.ssh\id_ed25519` |
| 公钥位置 | `C:\Users\zsgre\.ssh\id_ed25519.pub` |
| 密钥类型 | Ed25519（现代、安全） |
| 已添加到 GitHub 的密钥标题 | `WorkBuddy 笔记本` |
| 指纹 | `SHA256:4WWfXoOPXMREdshhfDr6CxrkRNTDFjBgXjyLydFU+Wk` |
| 连接验证结果 | `ssh -T git@github.com` → `Hi NOVASUN168! You've successfully authenticated` ✅ |

⚠️ **安全提醒**：私钥文件（`id_ed25519`）千万不要发给任何人，也不要上传到公开地方。换电脑时必须重新生成并添加 SSH 密钥。

## 三、远程仓库
| 项目 | 内容 |
|------|------|
| 仓库名 | `nova-business` |
| 可见性 | 公开（public） |
| SSH 地址 | `git@github.com:NOVASUN168/nova-business.git` |
| HTTPS 地址 | `https://github.com/NOVASUN168/nova-business.git` |

## 四、常用 git 命令速查
```bash
git status            # 查看当前改动
git add .             # 把所有改动加入暂存区
git commit -m "说明"  # 提交
git push              # 推送到 GitHub
git pull              # 从 GitHub 拉取最新
git clone <地址>      # 第一次下载仓库
```

## 五、WorkBuddy 里的 GitHub 连接器
- 状态：已连接（但为**受限权限**，只能查信息/读仓库，**不能创建仓库**）。
- 如果需要创建仓库，请在 GitHub 网页上操作（见下方步骤），或联系管理员调整连接器权限。
